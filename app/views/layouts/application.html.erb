<!DOCTYPE html>
<html>
	<head>
		<title>SEPTA - Is your Regional Rail train on time?</title>
		<link href='https://fonts.googleapis.com/css?family=Poiret+One|Josefin+Sans' rel='stylesheet' type='text/css'>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
			
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<%= csrf_meta_tags %>
	</head>
	<body>
		<div class="container">

		<p class="notice"><%= notice if !flash.empty?  %></p>
		<p class="alert"><%= alert if !flash.empty?  %></p>
		<div class="navbar">
			<% if user_signed_in? %>
				<%= link_to 'Log Out', destroy_user_session_path, method: :delete%>
			<% end %>
		</div>
		<div class="content">
			<%= yield %>
		</div>
		</div>
	</body>
</html>
